{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "API (cmd/api)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/cmd/api",
            "envFile": "${workspaceFolder}/.env",
            "env": {
                "MODE": "DEVELOP"
            }
        }
    ]
}
